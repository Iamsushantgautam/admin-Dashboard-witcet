<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= note.notesTitle %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f0f2f5;
    }
    .note-container {
      max-width: 900px;
      margin: 40px auto;
      background: white;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0,0,0,0.08);
    }
    .unit-box {
      background: #f9f9f9;
      border-left: 4px solid #0d6efd;
      padding: 15px;
      margin-bottom: 15px;
      border-radius: 6px;
    }
    .unit-box h6 {
      margin-bottom: 5px;
      color: #0d6efd;
    }
    .pdf-link {
      word-break: break-all;
    }
  </style>
</head>
<body>

  <div class="note-container">
    <h2 class="text-primary mb-2"><%= note.notesTitle %></h2>
    <h5 class="text-muted mb-4">( <%= note.notesCode %> )</h5>

    <div class="mb-4">
      <h5 class="text-dark fw-bold">Introduction:</h5>
      <p class="text-secondary"><%= note.introTitle %></p>
    </div>

    <h4 class="text-success mb-3">Units:</h4>
    <% note.units.forEach((unit, index) => { %>
      <div class="unit-box">
        <h6>Unit <%= index + 1 %>: <%= unit.unitTitle %></h6>
        <p><strong>Owner:</strong> <%= unit.ownerName %></p>
        <p class="pdf-link"><strong>PDF:</strong> <a href="<%= unit.pdfLink %>" target="_blank"><%= unit.pdfLink %></a></p>
      </div>
    <% }) %>
  </div>

</body>
</html>
