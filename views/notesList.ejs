<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>All Notes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
    }
    .note-card {
      background: white;
      padding: 20px;
      border-radius: 12px;
      box-shadow: 0 0 15px rgba(0,0,0,0.05);
      transition: transform 0.2s ease;
    }
    .note-card:hover {
      transform: scale(1.01);
    }
    .note-title {
      font-size: 1.25rem;
      color: #0d6efd;
      text-decoration: none;
    }
    .note-title:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <div class="container py-5">
    <h2 class="text-center text-primary mb-5">Available Notes</h2>

    <div class="row row-cols-1 row-cols-md-2 g-4">
      <% notes.forEach(note => { %>
        <div class="col">
          <div class="note-card p-3">
            <a href="/detailednotes/<%= note._id %>" class="note-title fw-bold d-block mb-2">
              <%= note.notesTitle %> (<%= note.notesCode %>)
            </a>
            <p class="text-muted mb-1"><strong>Intro:</strong> <%= note.introTitle %></p>
            <p class="text-secondary"><strong>Total Units:</strong> <%= note.units.length %></p>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

</body>
</html>
