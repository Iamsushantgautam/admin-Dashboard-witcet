<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Edit Note</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <style>
    body {
      background: linear-gradient(to right, #f8f9fa, #e0f7fa);
      min-height: 100vh;
      padding-top: 70px;
      padding-bottom: 40px;
    }

    .navbar {
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    .edit-form-container {
      background-color: white;
      padding: 40px 30px;
      border-radius: 16px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      max-width: 750px;
      width: 100%;
      margin: 0 auto;
      animation: slideIn 0.7s ease-in-out;
    }

    @keyframes slideIn {
      from {
        transform: translateY(50px);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    h2 {
      text-align: center;
      margin-bottom: 30px;
      font-weight: bold;
      color: #333;
    }

    .form-label {
      font-weight: 600;
    }

    .btn-update {
      background-color: #28a745;
      color: white;
      width: 100%;
      padding: 12px;
      font-size: 16px;
      border-radius: 8px;
      transition: background-color 0.3s ease;
    }

    .btn-update:hover {
      background-color: #1e7e34;
    }

    .form-control:focus {
      box-shadow: 0 0 0 0.2rem rgba(40, 167, 69, 0.25);
      transition: all 0.3s ease;
    }
  </style>
</head>
<body>

  <!-- ✅ Bootstrap Navigation -->

  <!-- Navbar Include -->
  <%- include('partials/navbar') %>

  <!-- ✅ Edit Note Form -->
  <div class="edit-form-container">
    <h2>Edit Note</h2>
    <form action="/edit/<%= note._id %>" method="POST">
      <div class="mb-3">
        <label class="form-label">Title</label>
        <input name="title" type="text" class="form-control" value="<%= note.title %>" required>
      </div>

      <div class="row">
        <div class="col-md-6 mb-3">
          <label class="form-label">Notes Page Path</label>
          <input name="notesPagePath" type="text" class="form-control" value="<%= note.notesPagePath %>">
        </div>
        <div class="col-md-6 mb-3">
          <label class="form-label">Image Path</label>
          <input name="imagePath" type="text" class="form-control" value="<%= note.imagePath %>">
        </div>
      </div>

      <div class="mb-3">
        <label class="form-label">Quantum Title</label>
        <input name="quantumTitle" type="text" class="form-control" value="<%= note.quantumTitle %>" required>
      </div>

      <div class="row">
        <div class="col-md-6 mb-3">
          <label class="form-label">Quantum Image Path</label>
          <input name="quantumImagePath" type="text" class="form-control" value="<%= note.quantumImagePath %>">
        </div>
        <div class="col-md-6 mb-3">
          <label class="form-label">Quantum Link</label>
          <input name="quantumLink" type="text" class="form-control" value="<%= note.quantumLink %>">
        </div>
      </div>

      <div class="mb-3">
        <label class="form-label">Tags</label>
        <input name="tag" type="text" class="form-control" value="<%= note.tag %>" required>
      </div>

      <div class="row">
        <div class="col-md-6 mb-3">
          <label class="form-label">PYQ Link</label>
          <input name="pyqLink" type="text" class="form-control" value="<%= note.pyqLink %>">
        </div>
        <div class="col-md-6 mb-3">
          <label class="form-label">PYQ Image</label>
          <input name="pyqImage" type="text" class="form-control" value="<%= note.pyqImage %>">
        </div>
      </div>

      <div class="mb-3">
        <label class="form-label">PYQ Title</label>
        <input name="pyqTitle" type="text" class="form-control" value="<%= note.pyqTitle %>">
      </div>

      <button type="submit" class="btn btn-update">Update Note</button>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
