<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Search Results</title>
  
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  
  <!-- Custom Styles -->
  <style>
    body {
      background: #f9f9f9;
      font-family: 'Segoe UI', sans-serif;
    }

    .card {
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
      border-radius: 15px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      animation: fadeInUp 0.6s ease both;
    }

    .card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
    }

    @keyframes fadeInUp {
      0% {
        opacity: 0;
        transform: translateY(20px);
      }
      100% {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .row {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 20px;
    }

    h2 {
      text-align: center;
      margin-top: 30px;
      color: #333;
    }

    .no-results {
      text-align: center;
      font-size: 1.2rem;
      color: #777;
      margin-top: 40px;
    }
  </style>
</head>

<body>

  <h2>Search Results</h2>

  <% if (notes.length === 0) { %>
    <p class="no-results">No matching notes found.</p>
  <% } else { %>
    <div class="container mt-4">
      <div class="row">
        <% notes.forEach(note => { %>
          <div class="card p-0" style="width: 18rem;">
            <img src="<%= note.imagePath %>" class="card-img-top" style="height: 180px; object-fit: cover;">
            <div class="card-body">
              <h5 class="card-title"><%= note.title %></h5>
              <p class="card-text text-muted"><%= note.tag %></p>
              <a href="<%= note.notesPagePath %>" class="btn btn-primary w-100">View Note</a>
            </div>
          </div>
        <% }) %>
      </div>
    </div>
  <% } %>

  <!-- Bootstrap JS (optional) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
